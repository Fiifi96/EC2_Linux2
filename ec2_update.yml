---
- hosts: ec2-18-188-195-221.us-east-2.compute.amazonaws.com
  become: true

  tasks:
  - name: Upgrade all packages
    yum:
      name: '*'
      state: latest

  - name: Install the latest version of Apache
    yum:
      name: httpd
      state: latest
